```cpp
    // ACS_BSBS -> Horizonal line
    // ACS_BULLET -> Like Dots
    // ACS_CKBOARD -> ▒▒▒▒▒
    // ACS_DARROW -> ┴┴┴┴┴┴
    // ACS_DEGREE -> °
    // ACS_DIAMOND -> ◆◆◆◆◆◆◆◆◆◆◆◆◆
    // ASC_GEQUAL -> ≥≥≥≥≥≥≥
    // ACS_LANTERN -> ␋␋␋␋␋␋
    // ACS_LEQUAL -> ≤≤≤≤≤
    // ACS_NEQUAL -> ≠≠≠≠≠≠≠≠≠
    // ACS_LTEE -> ├

    box(stdscr, ACS_VLINE, ACS_HLINE);

```